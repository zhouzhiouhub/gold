{
  "name": "gard",
  "version": "1.0.0",
  "private": true,
  "description": "Gold price dashboard",
  "main": "server.js",
  "scripts": {
    "start": "node --use-env-proxy server.js",
    "pages:dev": "npx wrangler pages dev public --compatibility-flags nodejs_compat"
  },
  "dependencies": {
    "express": "^4.21.2",
    "iconv-lite": "^0.6.3"
  }
}
